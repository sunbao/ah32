# Review (Draft) — bailian-multimodal-provider-v1

## Checklist

- [ ] Provider selection is deterministic and observable
- [ ] Strict mode has no silent downgrade
- [ ] No image bytes/base64 in chat or Plan JSON
- [ ] Asset deletion semantics are correct (success/cancel/disconnect/TTL)
- [ ] Logs contain trace_id and no sensitive payloads

