# Frontend environment example

# Quick start:
# - Keep defaults for local backend
# - Only change `VITE_API_BASE` when backend is deployed to another machine
#
# Enable Vite HMR (taskpane auto-refresh). Default OFF for WPS debugging stability.
VITE_ENABLE_HMR=false

# Backend base URL
VITE_API_BASE=http://localhost:5123

# API key (must match backend `AH32_API_KEY` when auth is enabled)
VITE_API_KEY=

# Show agent thoughts in streaming (debug only)
VITE_SHOW_THOUGHTS=false

# Show RAG hit summaries in streaming (debug only)
VITE_SHOW_RAG_HITS=false

# Stream idle timeout (ms). 0 = disable (recommended for long tasks).
VITE_STREAM_IDLE_TIMEOUT_MS=0

# Dev UI master switch (bench/debug widgets). Must be enabled at build time.
VITE_ENABLE_DEV_UI=false

# Debug switches
VITE_DEBUG=false
VITE_PERFORMANCE_MONITORING=false
