"""Development-only modules (bench/debug APIs).

These are intentionally separated from `ah32.server.*` so production deployments can
exclude them easily (and to keep core business logic clean).
"""

